<ul>
  {% for item in menu %}
    <li class="{{item.classes | join(' ')}}">
      <a href="{{item.get_link}}">{{item.title}}</a>
      {% include "components/Menu/Menu.twig" with {'menu': item.get_children} %}
    </li>
  {% endfor %}
</ul>
